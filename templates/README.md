# Project Templates

Future home for versioned project scaffolding templates.

## Planned

- Python ML project (pyproject.toml, .gitignore, CLAUDE.md stub, Dockerfile with GPU)
- Python API service (FastAPI skeleton)
- General Python (minimal pyproject.toml, venv setup)

## Status

Not yet implemented. Currently `newproj` scaffolds inline (git init, .gitignore, README.md, .venv). When templates have real content, `newproj` should reference them here rather than embedding scaffold logic in the script.
