#!/usr/bin/env bash
# Launch Claude Code with fixed thinking budget (disables adaptive reasoning).
# Use this instead of "claude" when you want deterministic thinking allocation
# and cannot risk zero-reasoning turns. See: github.com/anthropics/claude-code/issues/42796
exec env CLAUDE_CODE_DISABLE_ADAPTIVE_THINKING=1 claude "$@"
